const car = 'Honda Civic';

const statements = [
  `I drive a ${car}`,
  `I would like to own a ${car}`,
  `${car} is a great car`,
];

statements.forEach((statement) => {
  console.log(statement);
});
